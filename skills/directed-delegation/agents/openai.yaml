interface:
  display_name: "Directed Delegation"
  short_description: "Select, brief, and verify useful subagent work"
  default_prompt: "Use $directed-delegation to assign this work to subagents with clear inputs, boundaries, and acceptance criteria."

# Discovery supports requested delegation and concrete opportunities for parallel work.
policy:
  allow_implicit_invocation: true
