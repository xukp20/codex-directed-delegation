# GPT-5.6 Sol

Consider this preset for analysis, implementation, diagnosis, coordination, and review that require sustained synthesis within an understandable objective. It can lead work or execute a bounded assignment; the role follows the task.

## Settings

The [profile](../../assets/models/gpt-5.6-sol.toml) preserves `high` reasoning, nominal `272000` context, and standard service tier. The explicit context setting avoids inheriting a larger parent value. Nominal capacity is not a verified effective runtime window.

## Assignment guidance

State the outcome, relevant consumers, settled decisions, writable scope, and observable verification. Permit ordinary implementation choices while identifying material decisions that require returning to the lead. Use an independent reviewer for a stable candidate when the risk warrants it; routine iterations do not require a higher-cost model.

Prefer small stages when intermediate results expose useful evidence or enable independent work. Do not impose phases on a short, tightly coupled change. Return changed artifacts, actual checks, and unresolved issues rather than a general assurance of completion.

## Recommended engineering companion

For engineering assignments with meaningful scope or complexity choices, recommend `right-sized-engineering`. Working experience motivating this preset includes unnecessary abstractions, speculative defenses, and expanded scope; treat observed behavior as evidence to address, not an inevitable model trait.

When authorized, supply the installed skill path and current contract in the brief. Require a real consumer and reachable consequence for added mechanisms, and concrete evidence for blocking review findings. Do not copy the companion's full policy into the profile. Model selection alone does not activate it; carry existing task authorization forward using [engineering guidance](../engineering-guidance.md).

Basis: existing repository configuration and user working preferences, reviewed 2026-09-10. Apply the shared [precedence and verification rules](index.md).
